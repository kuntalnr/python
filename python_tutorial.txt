In the windows command shell pip name_of_package downloads a model. pip is a python's manager and pulls module either a built-in/from internet 
In python, the index starts from 0. 0:4 does not select 5 elements, rather 4 since the last is not selected. 